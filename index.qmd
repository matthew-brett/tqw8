# Title

```{python}
from ipywidgets import interact
```

```{python}
from IPython.display import display, Markdown
```

```{python}
def interactive_notation(a=1,
                         b=0.37,
                         c=58):
    display(Markdown("$\\vec{\\hat{y}} = b_1 * $ `education` + $b_2 * $ `income` +   $\\text{c} $"))

interact(interactive_notation, a = (-1, 1, 0.1), b = (-1, 1, 0.1), c = (-10, 10, 0.1))
```
